
  <%= form_with(id: "gen-search",url: pokemons_url, method: "get", local: true) do%>
    <div class="form-group">
      <%= label_tag(:search, "Select Gen:") %></br>
      <%= collection_select(:gen, :gen_id, gens, :id, :name, html_options: {class: "form-controll"})%>
      <%= submit_tag("Search")%>
    </div>
  <% end %></br>